import { NasaNumberPipe } from './nasa-number.pipe';

describe('NasaNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new NasaNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
